name: signlik_frontend
description: Front-end only Sign Language app (mock)
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  shared_preferences: ^2.1.1
  flutter_svg: ^2.0.7
  camera: ^0.10.6
  http: ^1.1.0
  image_picker: ^1.0.4
  video_player: ^2.7.0
  intl: ^0.20.2
  google_mlkit_pose_detection: ^0.14.0
  google_mlkit_commons: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

