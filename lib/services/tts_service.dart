class TtsService {
  static void speak(String text) {
    // mock: print to console. Replace with flutter_tts implementation later.
    // Example: FlutterTts().speak(text);
    // For now:
    // ignore: avoid_print
    print('TTS speak: $text');
  }
}
